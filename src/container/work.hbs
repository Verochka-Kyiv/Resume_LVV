{{!--
Це код container файлу для summary сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div class="p-5">
    {{#with main}}
    <main>
        <section>
            {{> title data = 'Work history' big = true}}
            <div>
                <ul>
                    {{#each works }}
                    <li>
                        <div> {{position}}</div>

                        <br>

                        <div>{{duration.from}} - {{#if duration.to}} {{duration.to}} {{else}} (still now) {{/if}}</div>

                        <br>

                        <div>
                            {{#if company.url}}
                            {{> link text = company.name href = company.url blank=true noLine = true}} </div>
                        {{else}}
                        <b>{{company.name}} </b>
                        {{/if}}
                        <br>
                        <br>
                        <div>
                            <strong> Projects ({{projectAmount}}) </strong>
                        </div>
                        <br>
                        <ul>
                            {{#each projects}}
                            <li>
                                <div>{{> link text = name href = url blank=true noLine = true}} </div>
                                <br>
                                <div>
                                    <p>Project About: {{about}}</p>
                                </div>
                                <br>
                                <em>Project stacks ({{stackAmoGnt}}):</em>
                                <br>
                                <div>
                                    <ul>
                                        {{#each stacks}}
                                        <li>{{name}}</li>
                                        {{/each}}
                                    </ul>
                                </div>
                                <div>
                                    <br>
                                    <em>Project awards: </em>
                                    <ul>
                                        {{#each awards}}
                                        <li>{{name}}</li>
                                        {{/each}}
                                    </ul>

                                </div>
                            </li>
                            {{/each}}
                        </ul>
                    </li>
                    <br>

                    {{/each}}
                </ul>
            </div>
        </section>
    </main>
    {{/with}}
    .
</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/big.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}